<!-- 一个 PR 只做一件事 -->

## 改了什么

<!-- 一句话说清改动范围，例如：修改 src/perception.py 聊天区指纹的比对逻辑 -->

## 为什么改

<!-- 关联 issue，例如：Closes #12 -->

## 怎么测的

<!-- 手动验证方式或日志片段；有实测数字就写实测数字 -->

- [ ] 守住核心原则：**纯只读**——不注入、不 hook、不解密微信数据（唯一例外是「填入」，走辅助功能接口，且没改回剪贴板 + Cmd+V 那条路，原因见 `src/fill.py` 顶部注释）
- [ ] 未引入硬编码的 API Key / 密码（密钥只能放仓库外的 `~/.config/jev-yaba-wechat/env`）
- [ ] 改了用户可见行为 → 已更新 README.md
