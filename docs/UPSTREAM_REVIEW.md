# 版本同步记录 · 2026-09-22

## 已同步版本

从 `f31bd7112624272644fd6889e2010b763d8e7ead` 合并至本次拉取的 master `bacd86b555a81cca329434b3d215f4a253424e56`，共 8 个提交。包含 v0.2.0 及其后续更新，项目版本为 0.2.0。采用 Git 合并，保留双方历史。

| 提交 | 已纳入的更新 |
|---|---|
| 08d2716 | 聊天区图像指纹跳过重复 OCR，静止/变化双档轮询，独立分析线程 |
| e4d9e02 | 简化 Issue/PR 模板；首页保留本产品的介绍和配置 |
| a977ae2 | 版本号更新为 0.2.0，锁文件版本随随后提交同步 |
| 51e752f | 构建图标依赖修复已核对；本项目用 sips/iconutil 生成品牌图标，不引入原 PyObjC 图标流程所需的 uv sync |
| 2c60b2d | 后台预热 Vision OCR 与本地判断模型 |
| 0d9d745 | 只有思考、没有回复正文时显示具体原因；全部话术失败时向面板传递错误 |
| 6f9c41a | 文档二维码位置调整；本项目保留自己的首页，不引入对方社群入口 |
| bacd86b | 消息停稳前提前判断，停稳后复用结果；候选生成仍在停稳后进行 |

## 保留与适配

- 保留 jev-哑巴微信名称、吉祥物、图标、浮窗配色、菜单栏、bundle ID 和独立配置目录。
- 保留 GPT Responses 请求与正文解析；新错误提示也覆盖 Responses，不推荐换到无关服务商的模型。
- 保留 Chat Completions 和 Anthropic 兼容路径。
- 品牌预览不启动提前判断线程、读屏或预热，不创建判断模型。
- 预热与判断/排序共用模型锁，避免模型预热前向计算与消息处理同时运行。
- MIT 许可与原版权说明保持不变。

## 验证范围

本次只做代码语法、构建脚本语法、离线接口回归与合并差异检查。不启动真实微信读取，不调用 GPT/TypeSafe，不下载模型，不执行真实填入，不运行完整应用或重新制作安装包。

上游 README 的耗时和 CPU 数据没有作为本项目的实测结果。真实聊天切换、暂停恢复、窗口重开及延迟表现仍待后续验收。

检查结果：8 项离线接口测试通过，12 个 Python 文件语法通过，3 个启动/构建脚本语法通过；合并冲突标记与 git diff 空白检查通过。
